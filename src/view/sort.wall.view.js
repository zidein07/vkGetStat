var app = app || {};
var Backbone = Backbone || {};

app.wallSort = Backbone.View.extend({
  el: '.container',
  initialize: function () {

  },
  render: function () {
    console.log('test');
  }
});